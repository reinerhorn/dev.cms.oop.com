-- Falls Tabellen schon existieren -> löschen
DROP TABLE IF EXISTS order_items;
DROP TABLE IF EXISTS orders;
DROP TABLE IF EXISTS products;
DROP TABLE IF EXISTS login_users;

-- Benutzer-Tabelle
CREATE TABLE login_users (
    id VARCHAR(36) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL PRIMARY KEY,
    username VARCHAR(255) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    role ENUM('customer','admin') DEFAULT 'customer',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- Produkte
CREATE TABLE products (
    id VARCHAR(36) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    price DECIMAL(10,2) NOT NULL,
    category_id VARCHAR(36),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- Bestellungen
CREATE TABLE orders (
    id VARCHAR(36) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL PRIMARY KEY,
    user_id VARCHAR(36) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL,
    total DECIMAL(10,2) NOT NULL,
    status ENUM('pending','paid','shipped','completed','cancelled') DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES login_users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- Bestell-Positionen
CREATE TABLE order_items (
    id VARCHAR(36) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL PRIMARY KEY,
    order_id VARCHAR(36) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL,
    product_id VARCHAR(36) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL,
    quantity INT NOT NULL,
    price DECIMAL(10,2) NOT NULL,
    FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE,
    FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;


-- Beispiel-Daten für Benutzer
-- Testdaten für Benutzer
INSERT INTO login_users (id, username, password, email, role)
VALUES
('11111111-111111111111', 'admin',    '$2y$10$abcdefghijklmnopqrstuv', 'admin@example.com', '0'),
('22222222-222222222222', 'max',      '$2y$10$abcdefghijklmnopqrstuv', 'max@example.com', '0'),
('33333333-333333333333', 'lisa',     '$2y$10$abcdefghijklmnopqrstuv', 'lisa@example.com', '0');

-- Testdaten für Produkte
INSERT INTO products (id, name, description, price, category_id)
VALUES
('aaaaaaa1-aaaaaaaaaaa1', 'Laptop Pro 15"', 'Leistungsstarker Laptop mit 16GB RAM', 1499.99, NULL),
('aaaaaaa2-aaaaaaaaaaa2', 'Smartphone X', 'Neustes Modell mit OLED Display', 999.99, NULL),
('aaaaaaa3-aaaaaaaaaaa3', 'Bluetooth Kopfhörer', 'Kabellose Over-Ear Kopfhörer', 199.99, NULL);

-- Testdaten für Bestellungen
INSERT INTO orders (id, user_id, total, status)
VALUES
('bbbbbbb1-bbbbbbbbbbb1', '22222222-222222222222', 1699.98, 'paid'),
('bbbbbbb2-bbbbbbbbbbb2', '33333333-333333333333', 199.99, 'pending');

-- Testdaten für Bestellpositionen
INSERT INTO order_items (id, order_id, product_id, quantity, price)
VALUES
('ccccccc1-ccccccccccc1', 'bbbbbbb1-bbbbbbbbbbb1', 'aaaaaaa1-aaaaaaaaaaa1', 1, 1499.99),
('ccccccc2-ccccccccccc2', 'bbbbbbb1-bbbbbbbbbbb1', 'aaaaaaa3-aaaaaaaaaaa3', 1, 199.99),
('ccccccc3-ccccccccccc3', 'bbbbbbb2-bbbbbbbbbbb2', 'aaaaaaa3-aaaaaaaaaaa3', 1, 199.99);
