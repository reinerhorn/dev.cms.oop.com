 -- 1️⃣ Fremdschlüssel löschen
ALTER TABLE translation DROP FOREIGN KEY fk_translation_language;

-- 2️⃣ Collation der Spalte in trans_language korrigieren
ALTER TABLE trans_language 
MODIFY id VARCHAR(10) 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- 3️⃣ Collation der Spalte in translation anpassen
ALTER TABLE translation 
MODIFY fk_language_id VARCHAR(10) 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- 4️⃣ Fremdschlüssel neu anlegen
ALTER TABLE translation 
ADD CONSTRAINT fk_translation_language
FOREIGN KEY (fk_language_id)
REFERENCES trans_language(id)
ON DELETE CASCADE
ON UPDATE CASCADE;